<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.betreuer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.example.betreuer.helper.GlobalContext"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="Betreuer"
        android:roundIcon="@mipmap/logo_rounded"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.betreuer.activity.ReceiveTutorialActivity">
            <!-- Register to handle file opening -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="*"
                    android:mimeType="*/*"
                    android:pathPattern=".*\\.tut"
                    android:scheme="file" />
            </intent-filter>
            <intent-filter
                android:icon="@mipmap/logo_rounded"
                android:label="@string/app_name"
                tools:ignore="AppLinkUrlError"
                android:scheme="http">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />

                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="*/*" />
                <!--
                    Work around Android's ugly primitive PatternMatcher
                    implementation that can't cope with finding a . early in
                    the path unless it's explicitly matched.
                -->
                <data android:host="*" />
                <data android:pathPattern=".*\\.tut" />
                <data android:pathPattern=".*\\..*\\.tut" />
                <data android:pathPattern=".*\\..*\\..*\\.tut" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.tut" />
                <!-- keep going if you need more -->
            </intent-filter>
        </activity>
        <activity android:name="com.example.betreuer.activity.ViewTutorialActivity" />
        <activity android:name="com.example.betreuer.activity.CreateNewStepActivity" />
        <activity android:name="com.example.betreuer.activity.CreateTutorialActivity" />
        <activity android:name="com.example.betreuer.activity.ViewTutorialsActivity" />
        <activity android:name="com.example.betreuer.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.betreuer.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>